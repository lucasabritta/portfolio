name: CI — Production build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  production_build:
    name: Production build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      
      - name: Build production docker image
        run: docker compose --profile prod build frontend-prod
